/* FOOTER RETRO */
.retro-footer {
    width: 100%;
    padding: 10px 20px;

    background: #e5e5d8;
    border-top: 4px solid #c9c9b8; /* borde superior */
    box-shadow: 0 -4px 0 #bfbfaf inset; /* inset negativo */
   

    display: flex;
    align-items: center;
    justify-content: space-between;

    font-family: "Courier New", monospace;

    position: fixed;
    bottom: 0;  /* ⬅ se queda abajo */
    left: 0;
    z-index: 9999;
}

/* TÍTULO + LOGO */
.nav-title {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 4px;

    font-size: 1.6rem;
    font-weight: bold;
    color: #222;
    text-shadow: 0 1px #fff;
}

.nav-title .icon {
    width: 70px;
    height: auto;
    display: block;
}

/* BOTONES */
.nav-buttons {
    display: flex;
    gap: 10px;
}

.retro-btn {
    background: #d6d6c8;
    border: 2px solid #a9a99b;
    padding: 6px 14px;
    cursor: pointer;

    color: #111;
    font-size: 0.9rem;
    font-family: "Courier New";

    box-shadow: 0 2px 0 #8b8b7a;
}

.retro-btn:active {
    box-shadow: 0 0 0 #8b8b7a;
    position: relative;
    top: 2px;
}

.retro-btn:hover {
    background: #e0dfd0;
    box-shadow:
        0 2px 0 #8b8b7a,
        0 0 12px rgba(255, 140, 0, 0.6);
    border-color: #c5a37b;
}

.retro-btn.flash {
    box-shadow:
        0 2px 0 #8b8b7a,
        0 0 18px rgba(255, 140, 0, 1),
        0 0 30px rgba(255, 100, 0, 0.8);
}

/* LED */
.nav-status {
    display: flex;
    align-items: center;
    gap: 6px;
}

.led {
    width: 14px;
    height: 14px;
    background: orange;
    border-radius: 50%;
    box-shadow: 0 0 6px rgba(255, 140, 0, 0.9);
    animation: blinkLed 1s infinite;
}

@keyframes blinkLed {
    0%, 100% { opacity: 0.2; }
    50% { opacity: 1; }
}

.status-text {
    color: #222;
    font-weight: bold;
}

/* RESPONSIVE */
@media (max-width: 600px) {
    .nav-title {
        font-size: 1.2rem;
    }

    .nav-title .icon {
        width: 40px;
    }
}
