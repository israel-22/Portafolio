 /*BOOTSCREEN*/


html, body {
    margin: 0;
    padding: 0;
    width: 100%;
    min-height: 100%;
    background: #000;
    font-family: 'Courier New', monospace;
    overflow-x: hidden;
}
/*CRT RECICLADO */
.crt-screen {
    width: 90vw;
    position: relative;
    max-width: 1400px;
    height: 80vh;
    max-height: 830px;
    left: -10px;

    padding: 25px 15px;
    border: 3px solid rgba(0, 255, 0, 0.25);
    border-radius: 12px;

    background: linear-gradient(
    180deg,
     rgba(0,25,0,0.95),
     rgba(0,0,0,0.95));

    box-shadow: 
        0 0 40px rgba(0, 255, 0, 0.25),
        inset 0 0 25px rgba(0, 255, 0, 0.15);

    overflow: hidden;

    margin: 20px auto;  /* <-- evita empujar al navbar */
    color: #00ff66;
    text-shadow: 0 0 6px #00ff66;
}


/* Scanlines */
.crt-screen::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(rgba(0, 255, 0, 0.08) 0.1em, transparent 0.1em);
    background-size: 100% 0.2em;
    pointer-events: none;
}


.crt-screen::after {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(0, 255, 0, 0.02);
    animation: flicker 0.15s infinite;
    pointer-events: none;
}
@keyframes flicker {
    0% { opacity: 0.95; }
    5% { opacity: 0.90; }
    10% { opacity: 0.85; }
    15% { opacity: 0.92; }
    20% { opacity: 0.98; }
    25% { opacity: 1; }
    100% { opacity: 1; }
}

/* Terminal text */
.terminal-text {
    font-size: 1.2rem;
    line-height: 1.5em;
    text-shadow: 0 0 6px rgba(0, 255, 0, 0.8);
    z-index: 2;
}

.cursor {
    display: inline-block;
    width: 10px;
    height: 20px;
    background-color: #00ff66;
    animation: blink 1s step-end infinite;
    margin-left: 2px;
    vertical-align: middle;
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
}

.glow {
    position: absolute;
    inset: 0;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(
        ellipse at center,
        rgba(0, 255, 0, 0.35) 0%,
        rgba(0, 255, 0, 0.1) 50%,
        rgba(0, 0, 0, 0) 70%
    );
    pointer-events: none;
}


/* Responsivo */
@media (max-width: 600px) {
    .crt-screen {
        width: 95%;
        height: 60vh;
        padding: 15px;
        border-radius: 10px;
    }

    .terminal-text {
        font-size: 1rem;
    }

    .cursor {
        width: 8px;
        height: 16px;
    }
}

/*CRT.CSS*/
/* ================================
   CRT DEFINITIVO — ANCHO COMPLETO
================================ */
.crt-screen {
      width: 90%;          /* antes estaba 100%, ajusta para tu formulario */
    max-width: 1200px;   /* evita que sea gigante en pantallas enormes */
    margin: 20px auto;   /* centrado automático */
    padding: 25px 15px;  /* reduce padding lateral si queda demasiado espacio */
    box-sizing: border-box;
    
    border: 3px solid rgba(0, 255, 0, 0.25);
    border-radius: 12px;
    padding-bottom: 45px;
    margin-bottom: 15px; /* espacio entre formulario y footer */

    /* 👉 BACKGROUND COMBINADO: CRT + Tubo de Vacío */
    background:
        radial-gradient(
            circle,
            rgba(0, 255, 100, 0.15) 0%,
            rgba(0, 255, 100, 0.07) 35%,
            rgba(0, 255, 100, 0.03) 60%,
            rgba(0, 255, 100, 0.01) 80%,
            rgba(0, 255, 100, 0) 100%
        ),
        linear-gradient(180deg, rgba(0, 15, 0, 0.93), rgba(0, 0, 0, 0.95));

    border: 3px solid rgba(0, 255, 0, 0.32);
    border-radius: 10px;

    box-shadow:
        0 0 45px rgba(0, 255, 0, 0.22),
        inset 0 0 40px rgba(0, 255, 0, 0.25),
        inset 0 0 120px rgba(0, 255, 0, 0.2),
        inset 0 0 220px rgba(0, 255, 0, 0.12);

    overflow: hidden;
    position: relative;
    margin: 20px auto 0 auto;  /* <-- evita empujar al navbar */
    color: #00ff88;
    text-shadow: 0 0 6px #00ff66;

    /* Ligero efecto pantalla curva */
    transform: perspective(900px) rotateX(1.7deg) scale(1.0);

    /* efecto CRT suave */
    filter: blur(0.2px);
    backdrop-filter: blur(0.8px);
}

/* SCANLINES */
.crt-screen::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
        rgba(0, 255, 0, 0.08) 0.1em,
        transparent 0.1em
    );
    background-size: 100% 0.20em;
    pointer-events: none;
    z-index: 2;
}

/* FLICKER */
.crt-screen::after {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(0, 255, 0, 0.03);
    animation: flicker 0.12s infinite;
    pointer-events: none;
    z-index: 3;
}

@keyframes flicker {
    0% { opacity: 0.92; }
    10% { opacity: 0.88; }
    20% { opacity: 0.94; }
    30% { opacity: 0.90; }
    40% { opacity: 0.97; }
    100% { opacity: 1; }
}

/* GLOW */
.glow {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: radial-gradient(
        circle at center,
        rgba(0, 255, 0, 0.25) 0%,
        rgba(0, 255, 0, 0.08) 45%,
        rgba(0, 0, 0, 0) 75%
    );
    pointer-events: none;
}

/* TEXTO */
.terminal-text {
    position: relative;
    z-index: 10;
    font-size: 1.15rem;
    line-height: 1.45em;
    text-shadow: 0 0 6px rgba(0, 255, 0, 0.85);
}
.terminal-content form {
    width: 100%;
    margin: 0;
}


/* CURSOR */
.cursor {
    display: inline-block;
    width: 10px;
    height: 18px;
    background-color: #00ff66;
    animation: blink 1s step-end infinite;
    box-shadow: 0 0 6px rgba(0,255,0,0.9);
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
}

/* RESPONSIVE */
@media (max-width: 600px) {
    .crt-screen {
        width: 100vw;
        padding: 14px;
        height: auto;
    }
}

